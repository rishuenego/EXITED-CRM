# Backend API URL
# For development with both servers running:
# - If proxy works: leave empty (VITE_API_URL=)
# - If proxy doesn't work: set to http://localhost:5000
#
# For production: set to your deployed backend URL
VITE_API_URL=http://localhost:5000
